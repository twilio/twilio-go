/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio Memory API
 * APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'CreateProfile'
type CreateProfileParams struct {
	//
	ProfileData *ProfileData `json:"ProfileData,omitempty"`
}

func (params *CreateProfileParams) SetProfileData(ProfileData ProfileData) *CreateProfileParams {
	params.ProfileData = &ProfileData
	return params
}

// Create a new profile and set initial traits. The request synchronously resolves identity and either creates a new profile ID or retrieves the associated canonical profile ID based on any provided identifier traits present in the request. The request must contain at least one trait that is promoted to an identifier in its trait group settings. Any additional traits are queued for asynchronous processing.
func (c *ApiService) CreateProfile(StoreId string, params *CreateProfileParams) (*CreateProfileResponse, error) {
	path := "/v1/Stores/{storeId}/Profiles"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.ProfileData != nil {
		b, err := json.Marshal(*params.ProfileData)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &CreateProfileResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// CreateProfileWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) CreateProfileWithMetadata(StoreId string, params *CreateProfileParams) (*metadata.ResourceMetadata[CreateProfileResponse], error) {
	path := "/v1/Stores/{storeId}/Profiles"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.ProfileData != nil {
		b, err := json.Marshal(*params.ProfileData)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &CreateProfileResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[CreateProfileResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Asynchronously delete the profile permanently and all associated identifiers and traits. This operation is irreversible. Downstream caches or analytical stores may take time to reflect the deletion. Use cautiously and consider a soft‑delete strategy if regulatory or recovery requirements apply.
func (c *ApiService) DeleteProfile(StoreId string, ProfileId string) (*DeleteProfileResponse, error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &DeleteProfileResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// DeleteProfileWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) DeleteProfileWithMetadata(StoreId string, ProfileId string) (*metadata.ResourceMetadata[DeleteProfileResponse], error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &DeleteProfileResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[DeleteProfileResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Optional parameters for the method 'FetchProfile'
type FetchProfileParams struct {
	// Comma separated list of trait group names to include.
	TraitGroups *string `json:"traitGroups,omitempty"`
}

func (params *FetchProfileParams) SetTraitGroups(TraitGroups string) *FetchProfileParams {
	params.TraitGroups = &TraitGroups
	return params
}

// Retrieve profile traits by profile ID. Use the `traitGroups` query parameter to restrict results to a comma-separated allow list of trait group names. For large sets of traits, prefer using the dedicated `/Traits` endpoint for pagination.
func (c *ApiService) FetchProfile(StoreId string, ProfileId string, params *FetchProfileParams) (*Profile, error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.TraitGroups != nil {
		data.Set("traitGroups", *params.TraitGroups)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &Profile{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// FetchProfileWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) FetchProfileWithMetadata(StoreId string, ProfileId string, params *FetchProfileParams) (*metadata.ResourceMetadata[Profile], error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.TraitGroups != nil {
		data.Set("traitGroups", *params.TraitGroups)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &Profile{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[Profile](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Optional parameters for the method 'ListProfiles'
type ListProfilesParams struct {
	// The maximum number of items to return per page, maximum of 1000.
	PageSize *int `json:"pageSize,omitempty"`
	// The token for the page of results to retrieve.
	PageToken *string `json:"pageToken,omitempty"`
	// Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
	OrderBy *string `json:"orderBy,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListProfilesParams) SetPageSize(PageSize int) *ListProfilesParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListProfilesParams) SetPageToken(PageToken string) *ListProfilesParams {
	params.PageToken = &PageToken
	return params
}
func (params *ListProfilesParams) SetOrderBy(OrderBy string) *ListProfilesParams {
	params.OrderBy = &OrderBy
	return params
}
func (params *ListProfilesParams) SetLimit(Limit int) *ListProfilesParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of Profiles records from the API. Request is executed immediately.
func (c *ApiService) PageProfiles(StoreId string, params *ListProfilesParams, pageToken string) (*ListProfilesResponse, error) {
	path := "/v1/Stores/{storeId}/Profiles"

	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}
	if params != nil && params.OrderBy != nil {
		data.Set("orderBy", *params.OrderBy)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListProfilesResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageProfilesWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageProfilesWithMetadata(StoreId string, params *ListProfilesParams, pageToken string) (*metadata.ResourceMetadata[ListProfilesResponse], error) {
	path := "/v1/Stores/{storeId}/Profiles"

	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}
	if params != nil && params.OrderBy != nil {
		data.Set("orderBy", *params.OrderBy)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListProfilesResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListProfilesResponse](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists Profiles records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListProfiles(StoreId string, params *ListProfilesParams) ([]string, error) {
	response, errors := c.StreamProfiles(StoreId, params)

	records := make([]string, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListProfilesWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListProfilesWithMetadata(StoreId string, params *ListProfilesParams) (*metadata.ResourceMetadata[[]string], error) {
	response, errors := c.StreamProfilesWithMetadata(StoreId, params)
	resource := response.GetResource()

	records := make([]string, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]string](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams Profiles records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamProfiles(StoreId string, params *ListProfilesParams) (chan string, chan error) {
	if params == nil {
		params = &ListProfilesParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan string, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageProfiles(StoreId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamProfiles(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamProfilesWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamProfilesWithMetadata(StoreId string, params *ListProfilesParams) (*metadata.ResourceMetadata[chan string], chan error) {
	if params == nil {
		params = &ListProfilesParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan string, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageProfilesWithMetadata(StoreId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamProfiles(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan string](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamProfiles(response *ListProfilesResponse, params *ListProfilesParams, recordChannel chan string, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Profiles
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL+"/v1/Stores/{storeId}/Profiles", response, c.getNextListProfilesResponse)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ListProfilesResponse)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextListProfilesResponse(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListProfilesResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}

// Optional parameters for the method 'PatchProfileTraits'
type PatchProfileTraitsParams struct {
	//
	ProfilePatch *ProfilePatch `json:"ProfilePatch,omitempty"`
}

func (params *PatchProfileTraitsParams) SetProfilePatch(ProfilePatch ProfilePatch) *PatchProfileTraitsParams {
	params.ProfilePatch = &ProfilePatch
	return params
}

// Merge one or more trait groups into an existing profile. Only the traits provided are added or updated; unspecified traits remain unchanged. Only pre-defined trait groups and traits configured for the memory store can be patched. To remove a trait entirely, set its value to null.
func (c *ApiService) PatchProfileTraits(StoreId string, ProfileId string, params *PatchProfileTraitsParams) (*PatchProfileTraitsResponse, error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.ProfilePatch != nil {
		b, err := json.Marshal(*params.ProfilePatch)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Patch(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &PatchProfileTraitsResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PatchProfileTraitsWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PatchProfileTraitsWithMetadata(StoreId string, ProfileId string, params *PatchProfileTraitsParams) (*metadata.ResourceMetadata[PatchProfileTraitsResponse], error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.ProfilePatch != nil {
		b, err := json.Marshal(*params.ProfilePatch)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Patch(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &PatchProfileTraitsResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[PatchProfileTraitsResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}
