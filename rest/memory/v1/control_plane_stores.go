/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio Memory API
 * APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'CreateStore'
type CreateStoreParams struct {
	//
	ServiceRequest *ServiceRequest `json:"ServiceRequest,omitempty"`
}

func (params *CreateStoreParams) SetServiceRequest(ServiceRequest ServiceRequest) *CreateStoreParams {
	params.ServiceRequest = &ServiceRequest
	return params
}

// Create a new Memory Store for the Twilio account. Accounts can have multiple memory stores, up to a maximum limit of 15. Each memory store will automatically provision other dependent resources as needed, including Conversational Intelligence capabilities.
func (c *ApiService) CreateStore(params *CreateStoreParams) (*CreateStoreResponse, error) {
	path := "/v1/ControlPlane/Stores"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.ServiceRequest != nil {
		b, err := json.Marshal(*params.ServiceRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &CreateStoreResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// CreateStoreWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) CreateStoreWithMetadata(params *CreateStoreParams) (*metadata.ResourceMetadata[CreateStoreResponse], error) {
	path := "/v1/ControlPlane/Stores"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.ServiceRequest != nil {
		b, err := json.Marshal(*params.ServiceRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &CreateStoreResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[CreateStoreResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Deletes the Memory Store and all associated resources including identity resolution settings, trait groups, profiles, traits, observations, and summaries.
func (c *ApiService) DeleteStore(StoreId string) (*DeleteStoreResponse, error) {
	path := "/v1/ControlPlane/Stores/{storeId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &DeleteStoreResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// DeleteStoreWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) DeleteStoreWithMetadata(StoreId string) (*metadata.ResourceMetadata[DeleteStoreResponse], error) {
	path := "/v1/ControlPlane/Stores/{storeId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &DeleteStoreResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[DeleteStoreResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Retrieve the details of a specific Memory Store by its unique ID.
func (c *ApiService) FetchStore(StoreId string) (*Store, error) {
	path := "/v1/ControlPlane/Stores/{storeId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &Store{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// FetchStoreWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) FetchStoreWithMetadata(StoreId string) (*metadata.ResourceMetadata[Store], error) {
	path := "/v1/ControlPlane/Stores/{storeId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &Store{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[Store](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Optional parameters for the method 'ListStores'
type ListStoresParams struct {
	// The maximum number of items to return per page, maximum of 100.
	PageSize *int `json:"pageSize,omitempty"`
	// The token for the page of results to retrieve.
	PageToken *string `json:"pageToken,omitempty"`
	// Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
	OrderBy *string `json:"orderBy,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListStoresParams) SetPageSize(PageSize int) *ListStoresParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListStoresParams) SetPageToken(PageToken string) *ListStoresParams {
	params.PageToken = &PageToken
	return params
}
func (params *ListStoresParams) SetOrderBy(OrderBy string) *ListStoresParams {
	params.OrderBy = &OrderBy
	return params
}
func (params *ListStoresParams) SetLimit(Limit int) *ListStoresParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of Stores records from the API. Request is executed immediately.
func (c *ApiService) PageStores(params *ListStoresParams, pageToken string) (*ServiceList, error) {
	path := "/v1/ControlPlane/Stores"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}
	if params != nil && params.OrderBy != nil {
		data.Set("orderBy", *params.OrderBy)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ServiceList{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageStoresWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageStoresWithMetadata(params *ListStoresParams, pageToken string) (*metadata.ResourceMetadata[ServiceList], error) {
	path := "/v1/ControlPlane/Stores"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}
	if params != nil && params.OrderBy != nil {
		data.Set("orderBy", *params.OrderBy)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ServiceList{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ServiceList](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists Stores records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListStores(params *ListStoresParams) ([]string, error) {
	response, errors := c.StreamStores(params)

	records := make([]string, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListStoresWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListStoresWithMetadata(params *ListStoresParams) (*metadata.ResourceMetadata[[]string], error) {
	response, errors := c.StreamStoresWithMetadata(params)
	resource := response.GetResource()

	records := make([]string, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]string](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams Stores records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamStores(params *ListStoresParams) (chan string, chan error) {
	if params == nil {
		params = &ListStoresParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan string, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageStores(params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamStores(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamStoresWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamStoresWithMetadata(params *ListStoresParams) (*metadata.ResourceMetadata[chan string], chan error) {
	if params == nil {
		params = &ListStoresParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan string, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageStoresWithMetadata(params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamStores(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan string](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamStores(response *ServiceList, params *ListStoresParams, recordChannel chan string, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Stores
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL+"/v1/ControlPlane/Stores", response, c.getNextServiceList)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ServiceList)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextServiceList(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ServiceList{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}

// Optional parameters for the method 'PatchStore'
type PatchStoreParams struct {
	// Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches the one provided, preventing accidental overwrites.
	IfMatch *string `json:"If-Match,omitempty"`
	//
	PatchStoreRequest *PatchStoreRequest `json:"PatchStoreRequest,omitempty"`
}

func (params *PatchStoreParams) SetIfMatch(IfMatch string) *PatchStoreParams {
	params.IfMatch = &IfMatch
	return params
}
func (params *PatchStoreParams) SetPatchStoreRequest(PatchStoreRequest PatchStoreRequest) *PatchStoreParams {
	params.PatchStoreRequest = &PatchStoreRequest
	return params
}

// Partially update a Memory Store. Only the fields provided in the request body will be updated.
func (c *ApiService) PatchStore(StoreId string, params *PatchStoreParams) (*PatchStoreResponse, error) {
	path := "/v1/ControlPlane/Stores/{storeId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.PatchStoreRequest != nil {
		b, err := json.Marshal(*params.PatchStoreRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	if params != nil && params.IfMatch != nil {
		headers["If-Match"] = *params.IfMatch
	}
	resp, err := c.requestHandler.Patch(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &PatchStoreResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PatchStoreWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PatchStoreWithMetadata(StoreId string, params *PatchStoreParams) (*metadata.ResourceMetadata[PatchStoreResponse], error) {
	path := "/v1/ControlPlane/Stores/{storeId}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.PatchStoreRequest != nil {
		b, err := json.Marshal(*params.PatchStoreRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	if params != nil && params.IfMatch != nil {
		headers["If-Match"] = *params.IfMatch
	}
	resp, err := c.requestHandler.Patch(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &PatchStoreResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[PatchStoreResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}
