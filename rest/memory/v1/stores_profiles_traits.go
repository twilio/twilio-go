/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio Memory API
 * APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'ListProfileTraits'
type ListProfileTraitsParams struct {
	// The maximum number of items to return per page, maximum of 1000.
	PageSize *int `json:"pageSize,omitempty"`
	// The token for the page of results to retrieve.
	PageToken *string `json:"pageToken,omitempty"`
	// Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
	OrderBy *string `json:"orderBy,omitempty"`
	// Comma separated list of trait group names to include.
	TraitGroups *string `json:"traitGroups,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListProfileTraitsParams) SetPageSize(PageSize int) *ListProfileTraitsParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListProfileTraitsParams) SetPageToken(PageToken string) *ListProfileTraitsParams {
	params.PageToken = &PageToken
	return params
}
func (params *ListProfileTraitsParams) SetOrderBy(OrderBy string) *ListProfileTraitsParams {
	params.OrderBy = &OrderBy
	return params
}
func (params *ListProfileTraitsParams) SetTraitGroups(TraitGroups string) *ListProfileTraitsParams {
	params.TraitGroups = &TraitGroups
	return params
}
func (params *ListProfileTraitsParams) SetLimit(Limit int) *ListProfileTraitsParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of ProfileTraits records from the API. Request is executed immediately.
func (c *ApiService) PageProfileTraits(StoreId string, ProfileId string, params *ListProfileTraitsParams, pageToken string) (*ListProfileTraitsResponse, error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Traits"

	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}
	if params != nil && params.OrderBy != nil {
		data.Set("orderBy", *params.OrderBy)
	}
	if params != nil && params.TraitGroups != nil {
		data.Set("traitGroups", *params.TraitGroups)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListProfileTraitsResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageProfileTraitsWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageProfileTraitsWithMetadata(StoreId string, ProfileId string, params *ListProfileTraitsParams, pageToken string) (*metadata.ResourceMetadata[ListProfileTraitsResponse], error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Traits"

	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}
	if params != nil && params.OrderBy != nil {
		data.Set("orderBy", *params.OrderBy)
	}
	if params != nil && params.TraitGroups != nil {
		data.Set("traitGroups", *params.TraitGroups)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListProfileTraitsResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListProfileTraitsResponse](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists ProfileTraits records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListProfileTraits(StoreId string, ProfileId string, params *ListProfileTraitsParams) ([]FullTrait, error) {
	response, errors := c.StreamProfileTraits(StoreId, ProfileId, params)

	records := make([]FullTrait, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListProfileTraitsWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListProfileTraitsWithMetadata(StoreId string, ProfileId string, params *ListProfileTraitsParams) (*metadata.ResourceMetadata[[]FullTrait], error) {
	response, errors := c.StreamProfileTraitsWithMetadata(StoreId, ProfileId, params)
	resource := response.GetResource()

	records := make([]FullTrait, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]FullTrait](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams ProfileTraits records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamProfileTraits(StoreId string, ProfileId string, params *ListProfileTraitsParams) (chan FullTrait, chan error) {
	if params == nil {
		params = &ListProfileTraitsParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan FullTrait, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageProfileTraits(StoreId, ProfileId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamProfileTraits(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamProfileTraitsWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamProfileTraitsWithMetadata(StoreId string, ProfileId string, params *ListProfileTraitsParams) (*metadata.ResourceMetadata[chan FullTrait], chan error) {
	if params == nil {
		params = &ListProfileTraitsParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan FullTrait, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageProfileTraitsWithMetadata(StoreId, ProfileId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamProfileTraits(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan FullTrait](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamProfileTraits(response *ListProfileTraitsResponse, params *ListProfileTraitsParams, recordChannel chan FullTrait, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Traits
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL+"/v1/Stores/{storeId}/Profiles/{profileId}/Traits", response, c.getNextListProfileTraitsResponse)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ListProfileTraitsResponse)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextListProfileTraitsResponse(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListProfileTraitsResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}
