/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio Memory API
 * APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'ListObservationRevisions'
type ListObservationRevisionsParams struct {
	// The maximum number of items to return per page, maximum of 1000.
	PageSize *int `json:"pageSize,omitempty"`
	// The token for the page of results to retrieve.
	PageToken *string `json:"pageToken,omitempty"`
	// Compression algorithms supported by the client (e.g., gzip, deflate, br)
	AcceptEncoding *string `json:"Accept-Encoding,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListObservationRevisionsParams) SetPageSize(PageSize int) *ListObservationRevisionsParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListObservationRevisionsParams) SetPageToken(PageToken string) *ListObservationRevisionsParams {
	params.PageToken = &PageToken
	return params
}
func (params *ListObservationRevisionsParams) SetAcceptEncoding(AcceptEncoding string) *ListObservationRevisionsParams {
	params.AcceptEncoding = &AcceptEncoding
	return params
}
func (params *ListObservationRevisionsParams) SetLimit(Limit int) *ListObservationRevisionsParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of ObservationRevisions records from the API. Request is executed immediately.
func (c *ApiService) PageObservationRevisions(StoreId string, ProfileId string, ObservationId string, params *ListObservationRevisionsParams, pageToken string) (*ListObservationRevisionsResponse, error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Observations/{observationId}/Revisions"

	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)
	path = strings.Replace(path, "{"+"observationId"+"}", ObservationId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListObservationRevisionsResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageObservationRevisionsWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageObservationRevisionsWithMetadata(StoreId string, ProfileId string, ObservationId string, params *ListObservationRevisionsParams, pageToken string) (*metadata.ResourceMetadata[ListObservationRevisionsResponse], error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Observations/{observationId}/Revisions"

	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)
	path = strings.Replace(path, "{"+"observationId"+"}", ObservationId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListObservationRevisionsResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListObservationRevisionsResponse](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists ObservationRevisions records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListObservationRevisions(StoreId string, ProfileId string, ObservationId string, params *ListObservationRevisionsParams) ([]ObservationInfo, error) {
	response, errors := c.StreamObservationRevisions(StoreId, ProfileId, ObservationId, params)

	records := make([]ObservationInfo, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListObservationRevisionsWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListObservationRevisionsWithMetadata(StoreId string, ProfileId string, ObservationId string, params *ListObservationRevisionsParams) (*metadata.ResourceMetadata[[]ObservationInfo], error) {
	response, errors := c.StreamObservationRevisionsWithMetadata(StoreId, ProfileId, ObservationId, params)
	resource := response.GetResource()

	records := make([]ObservationInfo, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]ObservationInfo](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams ObservationRevisions records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamObservationRevisions(StoreId string, ProfileId string, ObservationId string, params *ListObservationRevisionsParams) (chan ObservationInfo, chan error) {
	if params == nil {
		params = &ListObservationRevisionsParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan ObservationInfo, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageObservationRevisions(StoreId, ProfileId, ObservationId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamObservationRevisions(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamObservationRevisionsWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamObservationRevisionsWithMetadata(StoreId string, ProfileId string, ObservationId string, params *ListObservationRevisionsParams) (*metadata.ResourceMetadata[chan ObservationInfo], chan error) {
	if params == nil {
		params = &ListObservationRevisionsParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan ObservationInfo, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageObservationRevisionsWithMetadata(StoreId, ProfileId, ObservationId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamObservationRevisions(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan ObservationInfo](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamObservationRevisions(response *ListObservationRevisionsResponse, params *ListObservationRevisionsParams, recordChannel chan ObservationInfo, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Revisions
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL+"/v1/Stores/{storeId}/Profiles/{profileId}/Observations/{observationId}/Revisions", response, c.getNextListObservationRevisionsResponse)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ListObservationRevisionsResponse)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextListObservationRevisionsResponse(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListObservationRevisionsResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}
