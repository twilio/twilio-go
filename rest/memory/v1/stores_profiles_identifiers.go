/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio Memory API
 * APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'CreateProfileIdentifier'
type CreateProfileIdentifierParams struct {
	//
	Identifier *Identifier `json:"Identifier,omitempty"`
}

func (params *CreateProfileIdentifierParams) SetIdentifier(Identifier Identifier) *CreateProfileIdentifierParams {
	params.Identifier = &Identifier
	return params
}

// Asynchronously attach a new identifier value to a profile. When multiple values exist for an `idType`, ordering, limits, and uniqueness checks are enforced by the corresponding identifier settings `limitPolicy` and  reflect any normalization. If the identifier already exists and points to another profile, resolution rules or merge processes may apply. Use the identifier specific endpoint to inspect, update, or remove existing values.
func (c *ApiService) CreateProfileIdentifier(StoreId string, ProfileId string, params *CreateProfileIdentifierParams) (*CreateProfileIdentifierResponse, error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Identifiers"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.Identifier != nil {
		b, err := json.Marshal(*params.Identifier)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &CreateProfileIdentifierResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// CreateProfileIdentifierWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) CreateProfileIdentifierWithMetadata(StoreId string, ProfileId string, params *CreateProfileIdentifierParams) (*metadata.ResourceMetadata[CreateProfileIdentifierResponse], error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Identifiers"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.Identifier != nil {
		b, err := json.Marshal(*params.Identifier)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &CreateProfileIdentifierResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[CreateProfileIdentifierResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Optional parameters for the method 'DeleteProfileIdentifier'
type DeleteProfileIdentifierParams struct {
	// When true, removes every stored value for the identifier type in a single operation. Defaults to false.
	RemoveAll *bool `json:"removeAll,omitempty"`
}

func (params *DeleteProfileIdentifierParams) SetRemoveAll(RemoveAll bool) *DeleteProfileIdentifierParams {
	params.RemoveAll = &RemoveAll
	return params
}

// Asynchronously remove stored values for the identifier type. The optional `removeAll` query parameter allows bulk removal; otherwise the service selects a single value to delete using the identifier settings `limitPolicy`.
func (c *ApiService) DeleteProfileIdentifier(StoreId string, ProfileId string, IdType string, params *DeleteProfileIdentifierParams) (*DeleteProfileIdentifierResponse, error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Identifiers/{idType}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)
	path = strings.Replace(path, "{"+"idType"+"}", IdType, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.RemoveAll != nil {
		data.Set("removeAll", fmt.Sprint(*params.RemoveAll))
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &DeleteProfileIdentifierResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// DeleteProfileIdentifierWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) DeleteProfileIdentifierWithMetadata(StoreId string, ProfileId string, IdType string, params *DeleteProfileIdentifierParams) (*metadata.ResourceMetadata[DeleteProfileIdentifierResponse], error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Identifiers/{idType}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)
	path = strings.Replace(path, "{"+"idType"+"}", IdType, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.RemoveAll != nil {
		data.Set("removeAll", fmt.Sprint(*params.RemoveAll))
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &DeleteProfileIdentifierResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[DeleteProfileIdentifierResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Retrieve all stored values for a specific identifier type on a profile. Values are normalized according to the configured identifier settings and returned in the order determined by its `limitPolicy`.
func (c *ApiService) FetchProfileIdentifier(StoreId string, ProfileId string, IdType string) (*IdentifierSet, error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Identifiers/{idType}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)
	path = strings.Replace(path, "{"+"idType"+"}", IdType, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &IdentifierSet{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// FetchProfileIdentifierWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) FetchProfileIdentifierWithMetadata(StoreId string, ProfileId string, IdType string) (*metadata.ResourceMetadata[IdentifierSet], error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Identifiers/{idType}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)
	path = strings.Replace(path, "{"+"idType"+"}", IdType, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &IdentifierSet{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[IdentifierSet](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Optional parameters for the method 'ListProfileIdentifiers'
type ListProfileIdentifiersParams struct {
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
	// Max number of records to return in a page
	PageSize *int `json:"PageSize,omitempty"`
}

func (params *ListProfileIdentifiersParams) SetLimit(Limit int) *ListProfileIdentifiersParams {
	params.Limit = &Limit
	return params
}
func (params *ListProfileIdentifiersParams) SetPageSize(PageSize int) *ListProfileIdentifiersParams {
	params.PageSize = &PageSize
	return params
}

// Retrieve a single page of ProfileIdentifiers records from the API. Request is executed immediately.
func (c *ApiService) PageProfileIdentifiers(StoreId string, ProfileId string, params *ListProfileIdentifiersParams, pageToken string) (*ListProfileIdentifiersResponse, error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Identifiers"

	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("PageSize", fmt.Sprint(*params.PageSize))
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListProfileIdentifiersResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageProfileIdentifiersWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageProfileIdentifiersWithMetadata(StoreId string, ProfileId string, params *ListProfileIdentifiersParams, pageToken string) (*metadata.ResourceMetadata[ListProfileIdentifiersResponse], error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Identifiers"

	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("PageSize", fmt.Sprint(*params.PageSize))
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListProfileIdentifiersResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListProfileIdentifiersResponse](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists ProfileIdentifiers records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListProfileIdentifiers(StoreId string, ProfileId string, params *ListProfileIdentifiersParams) ([]IdentifierSet, error) {
	response, errors := c.StreamProfileIdentifiers(StoreId, ProfileId, params)

	records := make([]IdentifierSet, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListProfileIdentifiersWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListProfileIdentifiersWithMetadata(StoreId string, ProfileId string, params *ListProfileIdentifiersParams) (*metadata.ResourceMetadata[[]IdentifierSet], error) {
	response, errors := c.StreamProfileIdentifiersWithMetadata(StoreId, ProfileId, params)
	resource := response.GetResource()

	records := make([]IdentifierSet, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]IdentifierSet](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams ProfileIdentifiers records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamProfileIdentifiers(StoreId string, ProfileId string, params *ListProfileIdentifiersParams) (chan IdentifierSet, chan error) {
	if params == nil {
		params = &ListProfileIdentifiersParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan IdentifierSet, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageProfileIdentifiers(StoreId, ProfileId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamProfileIdentifiers(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamProfileIdentifiersWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamProfileIdentifiersWithMetadata(StoreId string, ProfileId string, params *ListProfileIdentifiersParams) (*metadata.ResourceMetadata[chan IdentifierSet], chan error) {
	if params == nil {
		params = &ListProfileIdentifiersParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan IdentifierSet, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageProfileIdentifiersWithMetadata(StoreId, ProfileId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamProfileIdentifiers(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan IdentifierSet](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamProfileIdentifiers(response *ListProfileIdentifiersResponse, params *ListProfileIdentifiersParams, recordChannel chan IdentifierSet, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Identifiers
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL+"/v1/Stores/{storeId}/Profiles/{profileId}/Identifiers", response, c.getNextListProfileIdentifiersResponse)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ListProfileIdentifiersResponse)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextListProfileIdentifiersResponse(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListProfileIdentifiersResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}

// Optional parameters for the method 'PatchProfileIdentifier'
type PatchProfileIdentifierParams struct {
	//
	IdentifierUpdate *IdentifierUpdate `json:"IdentifierUpdate,omitempty"`
}

func (params *PatchProfileIdentifierParams) SetIdentifierUpdate(IdentifierUpdate IdentifierUpdate) *PatchProfileIdentifierParams {
	params.IdentifierUpdate = &IdentifierUpdate
	return params
}

// Asynchronously replace one of the stored values associated with the identifier type on a profile. `newValue` is normalized according to the configured identifier settings.   If the new identifier already exists and points to another profile, merge processes may apply.
func (c *ApiService) PatchProfileIdentifier(StoreId string, ProfileId string, IdType string, params *PatchProfileIdentifierParams) (*PatchProfileIdentifierResponse, error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Identifiers/{idType}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)
	path = strings.Replace(path, "{"+"idType"+"}", IdType, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.IdentifierUpdate != nil {
		b, err := json.Marshal(*params.IdentifierUpdate)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Patch(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &PatchProfileIdentifierResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PatchProfileIdentifierWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PatchProfileIdentifierWithMetadata(StoreId string, ProfileId string, IdType string, params *PatchProfileIdentifierParams) (*metadata.ResourceMetadata[PatchProfileIdentifierResponse], error) {
	path := "/v1/Stores/{storeId}/Profiles/{profileId}/Identifiers/{idType}"
	path = strings.Replace(path, "{"+"storeId"+"}", StoreId, -1)
	path = strings.Replace(path, "{"+"profileId"+"}", ProfileId, -1)
	path = strings.Replace(path, "{"+"idType"+"}", IdType, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.IdentifierUpdate != nil {
		b, err := json.Marshal(*params.IdentifierUpdate)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Patch(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &PatchProfileIdentifierResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[PatchProfileIdentifierResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}
