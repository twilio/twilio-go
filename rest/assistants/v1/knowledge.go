/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio - Assistants
 * This is the public Twilio REST API.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'CreateKnowledge'
type CreateKnowledgeParams struct {
	//
	AssistantsV1CreateKnowledgeRequest *AssistantsV1CreateKnowledgeRequest `json:"AssistantsV1CreateKnowledgeRequest,omitempty"`
}

func (params *CreateKnowledgeParams) SetAssistantsV1CreateKnowledgeRequest(AssistantsV1CreateKnowledgeRequest AssistantsV1CreateKnowledgeRequest) *CreateKnowledgeParams {
	params.AssistantsV1CreateKnowledgeRequest = &AssistantsV1CreateKnowledgeRequest
	return params
}

// Create knowledge
func (c *ApiService) CreateKnowledge(params *CreateKnowledgeParams) (*AssistantsV1Knowledge, error) {
	path := "/v1/Knowledge"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.AssistantsV1CreateKnowledgeRequest != nil {
		b, err := json.Marshal(*params.AssistantsV1CreateKnowledgeRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &AssistantsV1Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// CreateKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) CreateKnowledgeWithMetadata(params *CreateKnowledgeParams) (*metadata.ResourceMetadata[AssistantsV1Knowledge], error) {
	path := "/v1/Knowledge"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.AssistantsV1CreateKnowledgeRequest != nil {
		b, err := json.Marshal(*params.AssistantsV1CreateKnowledgeRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &AssistantsV1Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[AssistantsV1Knowledge](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Delete knowledge
func (c *ApiService) DeleteKnowledge(Id string) error {
	path := "/v1/Knowledge/{id}"
	path = strings.Replace(path, "{"+"id"+"}", Id, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers)
	if err != nil {
		return err
	}

	defer resp.Body.Close()

	return nil
}

// DeleteKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) DeleteKnowledgeWithMetadata(Id string) (*metadata.ResourceMetadata[bool], error) {
	path := "/v1/Knowledge/{id}"
	path = strings.Replace(path, "{"+"id"+"}", Id, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	metadataWrapper := metadata.NewResourceMetadata[bool](
		true,            // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Get knowledge
func (c *ApiService) FetchKnowledge(Id string) (*AssistantsV1Knowledge, error) {
	path := "/v1/Knowledge/{id}"
	path = strings.Replace(path, "{"+"id"+"}", Id, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &AssistantsV1Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// FetchKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) FetchKnowledgeWithMetadata(Id string) (*metadata.ResourceMetadata[AssistantsV1Knowledge], error) {
	path := "/v1/Knowledge/{id}"
	path = strings.Replace(path, "{"+"id"+"}", Id, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &AssistantsV1Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[AssistantsV1Knowledge](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Optional parameters for the method 'ListKnowledge'
type ListKnowledgeParams struct {
	//
	AssistantId *string `json:"AssistantId,omitempty"`
	// How many resources to return in each list page. The default is 50, and the maximum is 1000.
	PageSize *int `json:"PageSize,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListKnowledgeParams) SetAssistantId(AssistantId string) *ListKnowledgeParams {
	params.AssistantId = &AssistantId
	return params
}
func (params *ListKnowledgeParams) SetPageSize(PageSize int) *ListKnowledgeParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListKnowledgeParams) SetLimit(Limit int) *ListKnowledgeParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of Knowledge records from the API. Request is executed immediately.
func (c *ApiService) PageKnowledge(params *ListKnowledgeParams, pageToken, pageNumber string) (*ListKnowledgeResponse, error) {
	path := "/v1/Knowledge"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.AssistantId != nil {
		data.Set("AssistantId", *params.AssistantId)
	}
	if params != nil && params.PageSize != nil {
		data.Set("PageSize", fmt.Sprint(*params.PageSize))
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}
	if pageNumber != "" {
		data.Set("Page", pageNumber)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListKnowledgeResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageKnowledgeWithMetadata(params *ListKnowledgeParams, pageToken, pageNumber string) (*metadata.ResourceMetadata[ListKnowledgeResponse], error) {
	path := "/v1/Knowledge"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.AssistantId != nil {
		data.Set("AssistantId", *params.AssistantId)
	}
	if params != nil && params.PageSize != nil {
		data.Set("PageSize", fmt.Sprint(*params.PageSize))
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}
	if pageNumber != "" {
		data.Set("Page", pageNumber)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListKnowledgeResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListKnowledgeResponse](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists Knowledge records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListKnowledge(params *ListKnowledgeParams) ([]AssistantsV1Knowledge, error) {
	response, errors := c.StreamKnowledge(params)

	records := make([]AssistantsV1Knowledge, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListKnowledgeWithMetadata(params *ListKnowledgeParams) (*metadata.ResourceMetadata[[]AssistantsV1Knowledge], error) {
	response, errors := c.StreamKnowledgeWithMetadata(params)
	resource := response.GetResource()

	records := make([]AssistantsV1Knowledge, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]AssistantsV1Knowledge](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams Knowledge records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamKnowledge(params *ListKnowledgeParams) (chan AssistantsV1Knowledge, chan error) {
	if params == nil {
		params = &ListKnowledgeParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan AssistantsV1Knowledge, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageKnowledge(params, "", "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamKnowledge(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamKnowledgeWithMetadata(params *ListKnowledgeParams) (*metadata.ResourceMetadata[chan AssistantsV1Knowledge], chan error) {
	if params == nil {
		params = &ListKnowledgeParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan AssistantsV1Knowledge, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageKnowledgeWithMetadata(params, "", "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamKnowledge(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan AssistantsV1Knowledge](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamKnowledge(response *ListKnowledgeResponse, params *ListKnowledgeParams, recordChannel chan AssistantsV1Knowledge, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Knowledge
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL, response, c.getNextListKnowledgeResponse)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ListKnowledgeResponse)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextListKnowledgeResponse(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListKnowledgeResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}

// Optional parameters for the method 'UpdateKnowledge'
type UpdateKnowledgeParams struct {
	//
	AssistantsV1UpdateKnowledgeRequest *AssistantsV1UpdateKnowledgeRequest `json:"AssistantsV1UpdateKnowledgeRequest,omitempty"`
}

func (params *UpdateKnowledgeParams) SetAssistantsV1UpdateKnowledgeRequest(AssistantsV1UpdateKnowledgeRequest AssistantsV1UpdateKnowledgeRequest) *UpdateKnowledgeParams {
	params.AssistantsV1UpdateKnowledgeRequest = &AssistantsV1UpdateKnowledgeRequest
	return params
}

// Update knowledge
func (c *ApiService) UpdateKnowledge(Id string, params *UpdateKnowledgeParams) (*AssistantsV1Knowledge, error) {
	path := "/v1/Knowledge/{id}"
	path = strings.Replace(path, "{"+"id"+"}", Id, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.AssistantsV1UpdateKnowledgeRequest != nil {
		b, err := json.Marshal(*params.AssistantsV1UpdateKnowledgeRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Put(c.baseURL+path, data, headers, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &AssistantsV1Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// UpdateKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) UpdateKnowledgeWithMetadata(Id string, params *UpdateKnowledgeParams) (*metadata.ResourceMetadata[AssistantsV1Knowledge], error) {
	path := "/v1/Knowledge/{id}"
	path = strings.Replace(path, "{"+"id"+"}", Id, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.AssistantsV1UpdateKnowledgeRequest != nil {
		b, err := json.Marshal(*params.AssistantsV1UpdateKnowledgeRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Put(c.baseURL+path, data, headers, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &AssistantsV1Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[AssistantsV1Knowledge](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}
