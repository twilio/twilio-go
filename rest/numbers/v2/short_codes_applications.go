/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio - Numbers
 * This is the public Twilio REST API.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
)

// Optional parameters for the method 'CreateShortCodeApplication'
type CreateShortCodeApplicationParams struct {
	//
	CreateShortCodeApplicationRequest *CreateShortCodeApplicationRequest `json:"CreateShortCodeApplicationRequest,omitempty"`
}

func (params *CreateShortCodeApplicationParams) SetCreateShortCodeApplicationRequest(CreateShortCodeApplicationRequest CreateShortCodeApplicationRequest) *CreateShortCodeApplicationParams {
	params.CreateShortCodeApplicationRequest = &CreateShortCodeApplicationRequest
	return params
}

// Create a new short code application for an account
func (c *ApiService) CreateShortCodeApplication(params *CreateShortCodeApplicationParams) (*CreateShortCodeApplicationResponse, error) {
	path := "/v2/ShortCodes/Applications"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.CreateShortCodeApplicationRequest != nil {
		b, err := json.Marshal(*params.CreateShortCodeApplicationRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &CreateShortCodeApplicationResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// Fetch a specific Short Code Application instance.
func (c *ApiService) FetchShortCodeApplication(Sid string) (*ShortCodeApplication, error) {
	path := "/v2/ShortCodes/Applications/{sid}"
	path = strings.Replace(path, "{"+"sid"+"}", Sid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ShortCodeApplication{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// Optional parameters for the method 'ListShortCodeApplications'
type ListShortCodeApplicationsParams struct {
	// How many resources to return in each list page. The default is 50, and the maximum is 50.
	PageSize *int `json:"PageSize,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListShortCodeApplicationsParams) SetPageSize(PageSize int) *ListShortCodeApplicationsParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListShortCodeApplicationsParams) SetLimit(Limit int) *ListShortCodeApplicationsParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of ShortCodeApplications records from the API. Request is executed immediately.
func (c *ApiService) PageShortCodeApplications(params *ListShortCodeApplicationsParams, pageToken, pageNumber string) (*ShortCodeApplicationResponsePage, error) {
	path := "/v2/ShortCodes/Applications"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("PageSize", fmt.Sprint(*params.PageSize))
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}
	if pageNumber != "" {
		data.Set("Page", pageNumber)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ShortCodeApplicationResponsePage{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// Lists ShortCodeApplications records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListShortCodeApplications(params *ListShortCodeApplicationsParams) ([]ShortCodeApplication, error) {
	response, errors := c.StreamShortCodeApplications(params)

	records := make([]ShortCodeApplication, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// Streams ShortCodeApplications records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamShortCodeApplications(params *ListShortCodeApplicationsParams) (chan ShortCodeApplication, chan error) {
	if params == nil {
		params = &ListShortCodeApplicationsParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan ShortCodeApplication, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageShortCodeApplications(params, "", "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamShortCodeApplications(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

func (c *ApiService) streamShortCodeApplications(response *ShortCodeApplicationResponsePage, params *ListShortCodeApplicationsParams, recordChannel chan ShortCodeApplication, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Results
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL, response, c.getNextShortCodeApplicationResponsePage)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ShortCodeApplicationResponsePage)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextShortCodeApplicationResponsePage(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ShortCodeApplicationResponsePage{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}
