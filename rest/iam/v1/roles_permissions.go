/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio - Iam
 * This is the public Twilio REST API.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'ListRolePermission'
type ListRolePermissionParams struct {
	// How many resources to return in each list page. The default is 50, and the maximum is 100.
	PageSize *int `json:"PageSize,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListRolePermissionParams) SetPageSize(PageSize int) *ListRolePermissionParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListRolePermissionParams) SetLimit(Limit int) *ListRolePermissionParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of RolePermission records from the API. Request is executed immediately.
func (c *ApiService) PageRolePermission(RoleSid string, params *ListRolePermissionParams, pageToken, pageNumber string) (*ListRolePermissionResponse, error) {
	path := "/v1/Roles/{RoleSid}/Permissions"

	path = strings.Replace(path, "{"+"RoleSid"+"}", RoleSid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("PageSize", fmt.Sprint(*params.PageSize))
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}
	if pageNumber != "" {
		data.Set("Page", pageNumber)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListRolePermissionResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageRolePermissionWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageRolePermissionWithMetadata(RoleSid string, params *ListRolePermissionParams, pageToken, pageNumber string) (*metadata.ResourceMetadata[ListRolePermissionResponse], error) {
	path := "/v1/Roles/{RoleSid}/Permissions"

	path = strings.Replace(path, "{"+"RoleSid"+"}", RoleSid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("PageSize", fmt.Sprint(*params.PageSize))
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}
	if pageNumber != "" {
		data.Set("Page", pageNumber)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListRolePermissionResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListRolePermissionResponse](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists RolePermission records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListRolePermission(RoleSid string, params *ListRolePermissionParams) ([]IamV1RolePermission, error) {
	response, errors := c.StreamRolePermission(RoleSid, params)

	records := make([]IamV1RolePermission, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListRolePermissionWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListRolePermissionWithMetadata(RoleSid string, params *ListRolePermissionParams) (*metadata.ResourceMetadata[[]IamV1RolePermission], error) {
	response, errors := c.StreamRolePermissionWithMetadata(RoleSid, params)
	resource := response.GetResource()

	records := make([]IamV1RolePermission, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]IamV1RolePermission](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams RolePermission records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamRolePermission(RoleSid string, params *ListRolePermissionParams) (chan IamV1RolePermission, chan error) {
	if params == nil {
		params = &ListRolePermissionParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan IamV1RolePermission, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageRolePermission(RoleSid, params, "", "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamRolePermission(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamRolePermissionWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamRolePermissionWithMetadata(RoleSid string, params *ListRolePermissionParams) (*metadata.ResourceMetadata[chan IamV1RolePermission], chan error) {
	if params == nil {
		params = &ListRolePermissionParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan IamV1RolePermission, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageRolePermissionWithMetadata(RoleSid, params, "", "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamRolePermission(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan IamV1RolePermission](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamRolePermission(response *ListRolePermissionResponse, params *ListRolePermissionParams, recordChannel chan IamV1RolePermission, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Permissions
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL, response, c.getNextListRolePermissionResponse)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ListRolePermissionResponse)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextListRolePermissionResponse(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListRolePermissionResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}
