/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Conversation Orchestrator
 * Manage configurations, conversations, participants, and communications. Create configurations to define capture rules and channel settings, then use conversations to group related communications.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'CreateParticipantInConversation'
type CreateParticipantInConversationParams struct {
	//
	CreateParticipantInConversationRequest *CreateParticipantInConversationRequest `json:"CreateParticipantInConversationRequest,omitempty"`
}

func (params *CreateParticipantInConversationParams) SetCreateParticipantInConversationRequest(CreateParticipantInConversationRequest CreateParticipantInConversationRequest) *CreateParticipantInConversationParams {
	params.CreateParticipantInConversationRequest = &CreateParticipantInConversationRequest
	return params
}

// Create a Participant.
func (c *ApiService) CreateParticipantInConversation(ConversationSid string, params *CreateParticipantInConversationParams) (*ListParticipantByConversationResponseParticipants, error) {
	path := "/v2/Conversations/{ConversationSid}/Participants"
	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.CreateParticipantInConversationRequest != nil {
		b, err := json.Marshal(*params.CreateParticipantInConversationRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListParticipantByConversationResponseParticipants{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// CreateParticipantInConversationWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) CreateParticipantInConversationWithMetadata(ConversationSid string, params *CreateParticipantInConversationParams) (*metadata.ResourceMetadata[ListParticipantByConversationResponseParticipants], error) {
	path := "/v2/Conversations/{ConversationSid}/Participants"
	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.CreateParticipantInConversationRequest != nil {
		b, err := json.Marshal(*params.CreateParticipantInConversationRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListParticipantByConversationResponseParticipants{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListParticipantByConversationResponseParticipants](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Retrieve a Participant.
func (c *ApiService) FetchParticipant(ConversationSid string, Sid string) (*ListParticipantByConversationResponseParticipants, error) {
	path := "/v2/Conversations/{ConversationSid}/Participants/{Sid}"
	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)
	path = strings.Replace(path, "{"+"Sid"+"}", Sid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListParticipantByConversationResponseParticipants{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// FetchParticipantWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) FetchParticipantWithMetadata(ConversationSid string, Sid string) (*metadata.ResourceMetadata[ListParticipantByConversationResponseParticipants], error) {
	path := "/v2/Conversations/{ConversationSid}/Participants/{Sid}"
	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)
	path = strings.Replace(path, "{"+"Sid"+"}", Sid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListParticipantByConversationResponseParticipants{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListParticipantByConversationResponseParticipants](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Optional parameters for the method 'ListParticipantByConversation'
type ListParticipantByConversationParams struct {
	// Maximum number of items to return
	PageSize *int `json:"pageSize,omitempty"`
	// Page token for pagination
	PageToken *string `json:"pageToken,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListParticipantByConversationParams) SetPageSize(PageSize int) *ListParticipantByConversationParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListParticipantByConversationParams) SetPageToken(PageToken string) *ListParticipantByConversationParams {
	params.PageToken = &PageToken
	return params
}
func (params *ListParticipantByConversationParams) SetLimit(Limit int) *ListParticipantByConversationParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of ParticipantByConversation records from the API. Request is executed immediately.
func (c *ApiService) PageParticipantByConversation(ConversationSid string, params *ListParticipantByConversationParams, pageToken string) (*ListParticipantByConversationResponse, error) {
	path := "/v2/Conversations/{ConversationSid}/Participants"

	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListParticipantByConversationResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageParticipantByConversationWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageParticipantByConversationWithMetadata(ConversationSid string, params *ListParticipantByConversationParams, pageToken string) (*metadata.ResourceMetadata[ListParticipantByConversationResponse], error) {
	path := "/v2/Conversations/{ConversationSid}/Participants"

	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListParticipantByConversationResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListParticipantByConversationResponse](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists ParticipantByConversation records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListParticipantByConversation(ConversationSid string, params *ListParticipantByConversationParams) ([]ListParticipantByConversationResponseParticipants, error) {
	response, errors := c.StreamParticipantByConversation(ConversationSid, params)

	records := make([]ListParticipantByConversationResponseParticipants, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListParticipantByConversationWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListParticipantByConversationWithMetadata(ConversationSid string, params *ListParticipantByConversationParams) (*metadata.ResourceMetadata[[]ListParticipantByConversationResponseParticipants], error) {
	response, errors := c.StreamParticipantByConversationWithMetadata(ConversationSid, params)
	resource := response.GetResource()

	records := make([]ListParticipantByConversationResponseParticipants, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]ListParticipantByConversationResponseParticipants](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams ParticipantByConversation records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamParticipantByConversation(ConversationSid string, params *ListParticipantByConversationParams) (chan ListParticipantByConversationResponseParticipants, chan error) {
	if params == nil {
		params = &ListParticipantByConversationParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan ListParticipantByConversationResponseParticipants, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageParticipantByConversation(ConversationSid, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamParticipantByConversation(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamParticipantByConversationWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamParticipantByConversationWithMetadata(ConversationSid string, params *ListParticipantByConversationParams) (*metadata.ResourceMetadata[chan ListParticipantByConversationResponseParticipants], chan error) {
	if params == nil {
		params = &ListParticipantByConversationParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan ListParticipantByConversationResponseParticipants, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageParticipantByConversationWithMetadata(ConversationSid, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamParticipantByConversation(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan ListParticipantByConversationResponseParticipants](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamParticipantByConversation(response *ListParticipantByConversationResponse, params *ListParticipantByConversationParams, recordChannel chan ListParticipantByConversationResponseParticipants, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Participants
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL+"/v2/Conversations/{ConversationSid}/Participants", response, c.getNextListParticipantByConversationResponse)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ListParticipantByConversationResponse)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextListParticipantByConversationResponse(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListParticipantByConversationResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}

// Optional parameters for the method 'UpdateParticipantInConversation'
type UpdateParticipantInConversationParams struct {
	//
	UpdateParticipantInConversationRequest *UpdateParticipantInConversationRequest `json:"UpdateParticipantInConversationRequest,omitempty"`
}

func (params *UpdateParticipantInConversationParams) SetUpdateParticipantInConversationRequest(UpdateParticipantInConversationRequest UpdateParticipantInConversationRequest) *UpdateParticipantInConversationParams {
	params.UpdateParticipantInConversationRequest = &UpdateParticipantInConversationRequest
	return params
}

// Update an existing Participant
func (c *ApiService) UpdateParticipantInConversation(ConversationSid string, Sid string, params *UpdateParticipantInConversationParams) (*ListParticipantByConversationResponseParticipants, error) {
	path := "/v2/Conversations/{ConversationSid}/Participants/{Sid}"
	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)
	path = strings.Replace(path, "{"+"Sid"+"}", Sid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.UpdateParticipantInConversationRequest != nil {
		b, err := json.Marshal(*params.UpdateParticipantInConversationRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Put(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListParticipantByConversationResponseParticipants{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// UpdateParticipantInConversationWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) UpdateParticipantInConversationWithMetadata(ConversationSid string, Sid string, params *UpdateParticipantInConversationParams) (*metadata.ResourceMetadata[ListParticipantByConversationResponseParticipants], error) {
	path := "/v2/Conversations/{ConversationSid}/Participants/{Sid}"
	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)
	path = strings.Replace(path, "{"+"Sid"+"}", Sid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.UpdateParticipantInConversationRequest != nil {
		b, err := json.Marshal(*params.UpdateParticipantInConversationRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Put(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListParticipantByConversationResponseParticipants{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListParticipantByConversationResponseParticipants](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}
