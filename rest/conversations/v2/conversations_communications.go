/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Conversation Orchestrator
 * Manage configurations, conversations, participants, and communications. Create configurations to define capture rules and channel settings, then use conversations to group related communications.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'CreateCommunicationInConversation'
type CreateCommunicationInConversationParams struct {
	//
	CreateCommunicationInConversationRequest *CreateCommunicationInConversationRequest `json:"CreateCommunicationInConversationRequest,omitempty"`
}

func (params *CreateCommunicationInConversationParams) SetCreateCommunicationInConversationRequest(CreateCommunicationInConversationRequest CreateCommunicationInConversationRequest) *CreateCommunicationInConversationParams {
	params.CreateCommunicationInConversationRequest = &CreateCommunicationInConversationRequest
	return params
}

// Create a Communication.
func (c *ApiService) CreateCommunicationInConversation(ConversationSid string, params *CreateCommunicationInConversationParams) (*ListCommunicationByConversationResponseCommunications, error) {
	path := "/v2/Conversations/{ConversationSid}/Communications"
	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.CreateCommunicationInConversationRequest != nil {
		b, err := json.Marshal(*params.CreateCommunicationInConversationRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListCommunicationByConversationResponseCommunications{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// CreateCommunicationInConversationWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) CreateCommunicationInConversationWithMetadata(ConversationSid string, params *CreateCommunicationInConversationParams) (*metadata.ResourceMetadata[ListCommunicationByConversationResponseCommunications], error) {
	path := "/v2/Conversations/{ConversationSid}/Communications"
	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.CreateCommunicationInConversationRequest != nil {
		b, err := json.Marshal(*params.CreateCommunicationInConversationRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListCommunicationByConversationResponseCommunications{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListCommunicationByConversationResponseCommunications](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Retrieve a Communication.
func (c *ApiService) FetchCommunication(ConversationSid string, Sid string) (*ListCommunicationByConversationResponseCommunications, error) {
	path := "/v2/Conversations/{ConversationSid}/Communications/{Sid}"
	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)
	path = strings.Replace(path, "{"+"Sid"+"}", Sid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListCommunicationByConversationResponseCommunications{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// FetchCommunicationWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) FetchCommunicationWithMetadata(ConversationSid string, Sid string) (*metadata.ResourceMetadata[ListCommunicationByConversationResponseCommunications], error) {
	path := "/v2/Conversations/{ConversationSid}/Communications/{Sid}"
	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)
	path = strings.Replace(path, "{"+"Sid"+"}", Sid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListCommunicationByConversationResponseCommunications{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListCommunicationByConversationResponseCommunications](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Optional parameters for the method 'ListCommunicationByConversation'
type ListCommunicationByConversationParams struct {
	// Resource identifier to filter communications
	ChannelId *string `json:"channelId,omitempty"`
	// Maximum number of items to return
	PageSize *int `json:"pageSize,omitempty"`
	// Page token for pagination
	PageToken *string `json:"pageToken,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListCommunicationByConversationParams) SetChannelId(ChannelId string) *ListCommunicationByConversationParams {
	params.ChannelId = &ChannelId
	return params
}
func (params *ListCommunicationByConversationParams) SetPageSize(PageSize int) *ListCommunicationByConversationParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListCommunicationByConversationParams) SetPageToken(PageToken string) *ListCommunicationByConversationParams {
	params.PageToken = &PageToken
	return params
}
func (params *ListCommunicationByConversationParams) SetLimit(Limit int) *ListCommunicationByConversationParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of CommunicationByConversation records from the API. Request is executed immediately.
func (c *ApiService) PageCommunicationByConversation(ConversationSid string, params *ListCommunicationByConversationParams, pageToken string) (*ListCommunicationByConversationResponse, error) {
	path := "/v2/Conversations/{ConversationSid}/Communications"

	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.ChannelId != nil {
		data.Set("channelId", *params.ChannelId)
	}
	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListCommunicationByConversationResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageCommunicationByConversationWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageCommunicationByConversationWithMetadata(ConversationSid string, params *ListCommunicationByConversationParams, pageToken string) (*metadata.ResourceMetadata[ListCommunicationByConversationResponse], error) {
	path := "/v2/Conversations/{ConversationSid}/Communications"

	path = strings.Replace(path, "{"+"ConversationSid"+"}", ConversationSid, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.ChannelId != nil {
		data.Set("channelId", *params.ChannelId)
	}
	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListCommunicationByConversationResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListCommunicationByConversationResponse](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists CommunicationByConversation records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListCommunicationByConversation(ConversationSid string, params *ListCommunicationByConversationParams) ([]ListCommunicationByConversationResponseCommunications, error) {
	response, errors := c.StreamCommunicationByConversation(ConversationSid, params)

	records := make([]ListCommunicationByConversationResponseCommunications, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListCommunicationByConversationWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListCommunicationByConversationWithMetadata(ConversationSid string, params *ListCommunicationByConversationParams) (*metadata.ResourceMetadata[[]ListCommunicationByConversationResponseCommunications], error) {
	response, errors := c.StreamCommunicationByConversationWithMetadata(ConversationSid, params)
	resource := response.GetResource()

	records := make([]ListCommunicationByConversationResponseCommunications, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]ListCommunicationByConversationResponseCommunications](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams CommunicationByConversation records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamCommunicationByConversation(ConversationSid string, params *ListCommunicationByConversationParams) (chan ListCommunicationByConversationResponseCommunications, chan error) {
	if params == nil {
		params = &ListCommunicationByConversationParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan ListCommunicationByConversationResponseCommunications, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageCommunicationByConversation(ConversationSid, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamCommunicationByConversation(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamCommunicationByConversationWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamCommunicationByConversationWithMetadata(ConversationSid string, params *ListCommunicationByConversationParams) (*metadata.ResourceMetadata[chan ListCommunicationByConversationResponseCommunications], chan error) {
	if params == nil {
		params = &ListCommunicationByConversationParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan ListCommunicationByConversationResponseCommunications, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageCommunicationByConversationWithMetadata(ConversationSid, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamCommunicationByConversation(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan ListCommunicationByConversationResponseCommunications](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamCommunicationByConversation(response *ListCommunicationByConversationResponse, params *ListCommunicationByConversationParams, recordChannel chan ListCommunicationByConversationResponseCommunications, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Communications
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL+"/v2/Conversations/{ConversationSid}/Communications", response, c.getNextListCommunicationByConversationResponse)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ListCommunicationByConversationResponse)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextListCommunicationByConversationResponse(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListCommunicationByConversationResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}
