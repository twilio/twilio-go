/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio Knowledge API
 * APIs for managing knowledge bases and knowledge content for AI-powered applications.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'CreateKnowledge'
type CreateKnowledgeParams struct {
	//
	KnowledgeCore *KnowledgeCore `json:"KnowledgeCore,omitempty"`
}

func (params *CreateKnowledgeParams) SetKnowledgeCore(KnowledgeCore KnowledgeCore) *CreateKnowledgeParams {
	params.KnowledgeCore = &KnowledgeCore
	return params
}

// Create a new knowledge source from various data sources such as web content,  files, or raw text. The knowledge source will be processed and indexed to enable semantic search and retrieval.  ## Best practices  To maximize the effectiveness of Knowledge, consider the following best practices:  **Assess and optimize content:** Regularly evaluate your existing Knowledge sources for accuracy, relevance, and coverage. Identify any gaps or outdated information that could hinder the Assistant's performance.  **Simplify and structure content:** Ensure that the content is clear and concise. Use headings, bullet points, and metadata to make information straightforward to navigate for both the AI Assistant and your users.  **Prioritize high-impact content:** Focus on updating and maintaining content that is frequently accessed or critical to customer interactions. Consider using analytics to determine which Knowledge entries are most valuable.
func (c *ApiService) CreateKnowledge(KbId string, params *CreateKnowledgeParams) (*Knowledge, error) {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.KnowledgeCore != nil {
		b, err := json.Marshal(*params.KnowledgeCore)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// CreateKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) CreateKnowledgeWithMetadata(KbId string, params *CreateKnowledgeParams) (*metadata.ResourceMetadata[Knowledge], error) {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.KnowledgeCore != nil {
		b, err := json.Marshal(*params.KnowledgeCore)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[Knowledge](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Permanently delete knowledge source and all its associated data, including  processed chunks and embeddings. This action cannot be undone. The knowledge  resource will no longer be available for search or retrieval operations.
func (c *ApiService) DeleteKnowledge(KbId string, KnowledgeId string) error {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge/{knowledgeId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)
	path = strings.Replace(path, "{"+"knowledgeId"+"}", KnowledgeId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return err
	}

	defer resp.Body.Close()

	return nil
}

// DeleteKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) DeleteKnowledgeWithMetadata(KbId string, KnowledgeId string) (*metadata.ResourceMetadata[bool], error) {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge/{knowledgeId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)
	path = strings.Replace(path, "{"+"knowledgeId"+"}", KnowledgeId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	metadataWrapper := metadata.NewResourceMetadata[bool](
		true,            // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Fetch detailed information about a specific knowledge source by its ID.  This returns the complete knowledge source object including  processing status, source details, and configuration information.
func (c *ApiService) FetchKnowledge(KbId string, KnowledgeId string) (*Knowledge, error) {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge/{knowledgeId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)
	path = strings.Replace(path, "{"+"knowledgeId"+"}", KnowledgeId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// FetchKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) FetchKnowledgeWithMetadata(KbId string, KnowledgeId string) (*metadata.ResourceMetadata[Knowledge], error) {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge/{knowledgeId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)
	path = strings.Replace(path, "{"+"knowledgeId"+"}", KnowledgeId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[Knowledge](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Optional parameters for the method 'ListKnowledge'
type ListKnowledgeParams struct {
	// The page index. This value is simply for client state.
	Page *int `json:"page,omitempty"`
	// The maximum number of items to return per page, maximum of 1000.
	PageSize *int `json:"pageSize,omitempty"`
	// The token for the page of results to retrieve.
	PageToken *string `json:"pageToken,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListKnowledgeParams) SetPage(Page int) *ListKnowledgeParams {
	params.Page = &Page
	return params
}
func (params *ListKnowledgeParams) SetPageSize(PageSize int) *ListKnowledgeParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListKnowledgeParams) SetPageToken(PageToken string) *ListKnowledgeParams {
	params.PageToken = &PageToken
	return params
}
func (params *ListKnowledgeParams) SetLimit(Limit int) *ListKnowledgeParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of Knowledge records from the API. Request is executed immediately.
func (c *ApiService) PageKnowledge(KbId string, params *ListKnowledgeParams, pageToken string) (*ListKnowledgeResponse, error) {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge"

	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.Page != nil {
		data.Set("page", fmt.Sprint(*params.Page))
	}
	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListKnowledgeResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageKnowledgeWithMetadata(KbId string, params *ListKnowledgeParams, pageToken string) (*metadata.ResourceMetadata[ListKnowledgeResponse], error) {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge"

	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.Page != nil {
		data.Set("page", fmt.Sprint(*params.Page))
	}
	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListKnowledgeResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListKnowledgeResponse](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists Knowledge records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListKnowledge(KbId string, params *ListKnowledgeParams) ([]Knowledge, error) {
	response, errors := c.StreamKnowledge(KbId, params)

	records := make([]Knowledge, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListKnowledgeWithMetadata(KbId string, params *ListKnowledgeParams) (*metadata.ResourceMetadata[[]Knowledge], error) {
	response, errors := c.StreamKnowledgeWithMetadata(KbId, params)
	resource := response.GetResource()

	records := make([]Knowledge, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]Knowledge](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams Knowledge records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamKnowledge(KbId string, params *ListKnowledgeParams) (chan Knowledge, chan error) {
	if params == nil {
		params = &ListKnowledgeParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan Knowledge, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageKnowledge(KbId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamKnowledge(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamKnowledgeWithMetadata(KbId string, params *ListKnowledgeParams) (*metadata.ResourceMetadata[chan Knowledge], chan error) {
	if params == nil {
		params = &ListKnowledgeParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan Knowledge, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageKnowledgeWithMetadata(KbId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamKnowledge(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan Knowledge](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamKnowledge(response *ListKnowledgeResponse, params *ListKnowledgeParams, recordChannel chan Knowledge, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Knowledge
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL+"/v2/KnowledgeBases/{kbId}/Knowledge", response, c.getNextListKnowledgeResponse)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ListKnowledgeResponse)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextListKnowledgeResponse(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListKnowledgeResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}

// Optional parameters for the method 'PatchKnowledge'
type PatchKnowledgeParams struct {
	// When true, re-queues processing for this knowledge resource. Idempotent while the resource is already QUEUED or PROCESSING.
	Refresh *bool `json:"refresh,omitempty"`
	//
	KnowledgeCore *KnowledgeCore `json:"KnowledgeCore,omitempty"`
}

func (params *PatchKnowledgeParams) SetRefresh(Refresh bool) *PatchKnowledgeParams {
	params.Refresh = &Refresh
	return params
}
func (params *PatchKnowledgeParams) SetKnowledgeCore(KnowledgeCore KnowledgeCore) *PatchKnowledgeParams {
	params.KnowledgeCore = &KnowledgeCore
	return params
}

// Partially update mutable fields of an existing knowledge source such as name, description, tags, or source-specific configuration. Only the fields provided in the request body will be updated. Some changes (e.g., KnowledgeSourceTypes) may trigger asynchronous reprocessing of the underlying content. Fields omitted from the request remain unchanged. Immutable fields (id, type, status, url, createdAt, updatedAt) cannot be modified directly.  ## Refresh ##  To request reprocessing without changing fields, pass the query parameter `refresh=true`. When `refresh=true` is provided, the server will re-queue processing for this knowledge resource (transitioning the persisted status to `QUEUED`) and return 202 Accepted. This query parameter is idempotent while the resource is already QUEUED or PROCESSING.
func (c *ApiService) PatchKnowledge(KbId string, KnowledgeId string, params *PatchKnowledgeParams) (*Knowledge, error) {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge/{knowledgeId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)
	path = strings.Replace(path, "{"+"knowledgeId"+"}", KnowledgeId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	if params != nil && params.Refresh != nil {
		data.Set("refresh", fmt.Sprint(*params.Refresh))
	}
	body := []byte{}
	if params != nil && params.KnowledgeCore != nil {
		b, err := json.Marshal(*params.KnowledgeCore)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Patch(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PatchKnowledgeWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PatchKnowledgeWithMetadata(KbId string, KnowledgeId string, params *PatchKnowledgeParams) (*metadata.ResourceMetadata[Knowledge], error) {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge/{knowledgeId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)
	path = strings.Replace(path, "{"+"knowledgeId"+"}", KnowledgeId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	if params != nil && params.Refresh != nil {
		data.Set("refresh", fmt.Sprint(*params.Refresh))
	}
	body := []byte{}
	if params != nil && params.KnowledgeCore != nil {
		b, err := json.Marshal(*params.KnowledgeCore)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Patch(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &Knowledge{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[Knowledge](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}
