/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio Knowledge API
 * APIs for managing knowledge bases and knowledge content for AI-powered applications.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'CreateKnowledgeBase'
type CreateKnowledgeBaseParams struct {
	//
	KnowledgeBaseCore *KnowledgeBaseCore `json:"KnowledgeBaseCore,omitempty"`
}

func (params *CreateKnowledgeBaseParams) SetKnowledgeBaseCore(KnowledgeBaseCore KnowledgeBaseCore) *CreateKnowledgeBaseParams {
	params.KnowledgeBaseCore = &KnowledgeBaseCore
	return params
}

// Create a new Knowledge Base for the Twilio account. Accounts can have multiple knowledge bases. Each knowledge base can contain multiple knowledge resources such as documents, websites, or text content that can be used for context and information retrieval.
func (c *ApiService) CreateKnowledgeBase(params *CreateKnowledgeBaseParams) (*CreateKnowledgeBaseResponse, error) {
	path := "/v2/ControlPlane/KnowledgeBases"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.KnowledgeBaseCore != nil {
		b, err := json.Marshal(*params.KnowledgeBaseCore)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &CreateKnowledgeBaseResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// CreateKnowledgeBaseWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) CreateKnowledgeBaseWithMetadata(params *CreateKnowledgeBaseParams) (*metadata.ResourceMetadata[CreateKnowledgeBaseResponse], error) {
	path := "/v2/ControlPlane/KnowledgeBases"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.KnowledgeBaseCore != nil {
		b, err := json.Marshal(*params.KnowledgeBaseCore)
		if err != nil {
			return nil, err
		}
		body = b
	}

	resp, err := c.requestHandler.Post(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &CreateKnowledgeBaseResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[CreateKnowledgeBaseResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Delete a Knowledge Base and all associated knowledge resources. This action cannot be undone.
func (c *ApiService) DeleteKnowledgeBase(KbId string) (*DeleteKnowledgeBaseResponse, error) {
	path := "/v2/ControlPlane/KnowledgeBases/{kbId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &DeleteKnowledgeBaseResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// DeleteKnowledgeBaseWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) DeleteKnowledgeBaseWithMetadata(KbId string) (*metadata.ResourceMetadata[DeleteKnowledgeBaseResponse], error) {
	path := "/v2/ControlPlane/KnowledgeBases/{kbId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Delete(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &DeleteKnowledgeBaseResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[DeleteKnowledgeBaseResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Retrieve the details of a specific Knowledge Base by its unique ID.
func (c *ApiService) FetchKnowledgeBase(KbId string) (*KnowledgeBase, error) {
	path := "/v2/ControlPlane/KnowledgeBases/{kbId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &KnowledgeBase{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// FetchKnowledgeBaseWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) FetchKnowledgeBaseWithMetadata(KbId string) (*metadata.ResourceMetadata[KnowledgeBase], error) {
	path := "/v2/ControlPlane/KnowledgeBases/{kbId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &KnowledgeBase{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[KnowledgeBase](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Optional parameters for the method 'ListKnowledgeBases'
type ListKnowledgeBasesParams struct {
	// The maximum number of items to return per page, maximum of 100.
	PageSize *int `json:"pageSize,omitempty"`
	// The token for the page of results to retrieve.
	PageToken *string `json:"pageToken,omitempty"`
	// Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
	OrderBy *string `json:"orderBy,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListKnowledgeBasesParams) SetPageSize(PageSize int) *ListKnowledgeBasesParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListKnowledgeBasesParams) SetPageToken(PageToken string) *ListKnowledgeBasesParams {
	params.PageToken = &PageToken
	return params
}
func (params *ListKnowledgeBasesParams) SetOrderBy(OrderBy string) *ListKnowledgeBasesParams {
	params.OrderBy = &OrderBy
	return params
}
func (params *ListKnowledgeBasesParams) SetLimit(Limit int) *ListKnowledgeBasesParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of KnowledgeBases records from the API. Request is executed immediately.
func (c *ApiService) PageKnowledgeBases(params *ListKnowledgeBasesParams, pageToken string) (*KnowledgeBaseList, error) {
	path := "/v2/ControlPlane/KnowledgeBases"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}
	if params != nil && params.OrderBy != nil {
		data.Set("orderBy", *params.OrderBy)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &KnowledgeBaseList{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageKnowledgeBasesWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageKnowledgeBasesWithMetadata(params *ListKnowledgeBasesParams, pageToken string) (*metadata.ResourceMetadata[KnowledgeBaseList], error) {
	path := "/v2/ControlPlane/KnowledgeBases"

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}
	if params != nil && params.OrderBy != nil {
		data.Set("orderBy", *params.OrderBy)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &KnowledgeBaseList{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[KnowledgeBaseList](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists KnowledgeBases records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListKnowledgeBases(params *ListKnowledgeBasesParams) ([]KnowledgeBase, error) {
	response, errors := c.StreamKnowledgeBases(params)

	records := make([]KnowledgeBase, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListKnowledgeBasesWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListKnowledgeBasesWithMetadata(params *ListKnowledgeBasesParams) (*metadata.ResourceMetadata[[]KnowledgeBase], error) {
	response, errors := c.StreamKnowledgeBasesWithMetadata(params)
	resource := response.GetResource()

	records := make([]KnowledgeBase, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]KnowledgeBase](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams KnowledgeBases records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamKnowledgeBases(params *ListKnowledgeBasesParams) (chan KnowledgeBase, chan error) {
	if params == nil {
		params = &ListKnowledgeBasesParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan KnowledgeBase, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageKnowledgeBases(params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamKnowledgeBases(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamKnowledgeBasesWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamKnowledgeBasesWithMetadata(params *ListKnowledgeBasesParams) (*metadata.ResourceMetadata[chan KnowledgeBase], chan error) {
	if params == nil {
		params = &ListKnowledgeBasesParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan KnowledgeBase, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageKnowledgeBasesWithMetadata(params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamKnowledgeBases(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan KnowledgeBase](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamKnowledgeBases(response *KnowledgeBaseList, params *ListKnowledgeBasesParams, recordChannel chan KnowledgeBase, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.KnowledgeBases
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL+"/v2/ControlPlane/KnowledgeBases", response, c.getNextKnowledgeBaseList)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*KnowledgeBaseList)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextKnowledgeBaseList(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &KnowledgeBaseList{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}

// Optional parameters for the method 'PatchKnowledgeBase'
type PatchKnowledgeBaseParams struct {
	// Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches the one provided, preventing accidental overwrites.
	IfMatch *string `json:"If-Match,omitempty"`
	//
	PatchKnowledgeBaseRequest *PatchKnowledgeBaseRequest `json:"PatchKnowledgeBaseRequest,omitempty"`
}

func (params *PatchKnowledgeBaseParams) SetIfMatch(IfMatch string) *PatchKnowledgeBaseParams {
	params.IfMatch = &IfMatch
	return params
}
func (params *PatchKnowledgeBaseParams) SetPatchKnowledgeBaseRequest(PatchKnowledgeBaseRequest PatchKnowledgeBaseRequest) *PatchKnowledgeBaseParams {
	params.PatchKnowledgeBaseRequest = &PatchKnowledgeBaseRequest
	return params
}

// Partially update a Knowledge Base. Only the fields provided in the request body will be updated.
func (c *ApiService) PatchKnowledgeBase(KbId string, params *PatchKnowledgeBaseParams) (*UpdateKnowledgeBaseResponse, error) {
	path := "/v2/ControlPlane/KnowledgeBases/{kbId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.PatchKnowledgeBaseRequest != nil {
		b, err := json.Marshal(*params.PatchKnowledgeBaseRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	if params != nil && params.IfMatch != nil {
		headers["If-Match"] = *params.IfMatch
	}
	resp, err := c.requestHandler.Patch(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &UpdateKnowledgeBaseResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PatchKnowledgeBaseWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PatchKnowledgeBaseWithMetadata(KbId string, params *PatchKnowledgeBaseParams) (*metadata.ResourceMetadata[UpdateKnowledgeBaseResponse], error) {
	path := "/v2/ControlPlane/KnowledgeBases/{kbId}"
	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/json",
	}

	body := []byte{}
	if params != nil && params.PatchKnowledgeBaseRequest != nil {
		b, err := json.Marshal(*params.PatchKnowledgeBaseRequest)
		if err != nil {
			return nil, err
		}
		body = b
	}

	if params != nil && params.IfMatch != nil {
		headers["If-Match"] = *params.IfMatch
	}
	resp, err := c.requestHandler.Patch(c.baseURL+path, data, headers, c.apiVersion, body...)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &UpdateKnowledgeBaseResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[UpdateKnowledgeBaseResponse](
		*ps,             // The resource object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}
