/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio Knowledge API
 * APIs for managing knowledge bases and knowledge content for AI-powered applications.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package openapi

import (
	"encoding/json"
	"fmt"
	"net/url"
	"strings"

	"github.com/twilio/twilio-go/client"
	"github.com/twilio/twilio-go/client/metadata"
)

// Optional parameters for the method 'ListKnowledgeChunks'
type ListKnowledgeChunksParams struct {
	// How many resources to return in each list page. The default is 50, and the maximum is 1000.
	PageSize *int `json:"pageSize,omitempty"`
	// The page token. This is provided by the API.
	PageToken *string `json:"pageToken,omitempty"`
	// Max number of records to return.
	Limit *int `json:"limit,omitempty"`
}

func (params *ListKnowledgeChunksParams) SetPageSize(PageSize int) *ListKnowledgeChunksParams {
	params.PageSize = &PageSize
	return params
}
func (params *ListKnowledgeChunksParams) SetPageToken(PageToken string) *ListKnowledgeChunksParams {
	params.PageToken = &PageToken
	return params
}
func (params *ListKnowledgeChunksParams) SetLimit(Limit int) *ListKnowledgeChunksParams {
	params.Limit = &Limit
	return params
}

// Retrieve a single page of KnowledgeChunks records from the API. Request is executed immediately.
func (c *ApiService) PageKnowledgeChunks(KbId string, KnowledgeId string, params *ListKnowledgeChunksParams, pageToken string) (*ListKnowledgeChunksResponse, error) {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge/{knowledgeId}/Chunks"

	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)
	path = strings.Replace(path, "{"+"knowledgeId"+"}", KnowledgeId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListKnowledgeChunksResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	return ps, err
}

// PageKnowledgeChunksWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) PageKnowledgeChunksWithMetadata(KbId string, KnowledgeId string, params *ListKnowledgeChunksParams, pageToken string) (*metadata.ResourceMetadata[ListKnowledgeChunksResponse], error) {
	path := "/v2/KnowledgeBases/{kbId}/Knowledge/{knowledgeId}/Chunks"

	path = strings.Replace(path, "{"+"kbId"+"}", KbId, -1)
	path = strings.Replace(path, "{"+"knowledgeId"+"}", KnowledgeId, -1)

	data := url.Values{}
	headers := map[string]interface{}{
		"Content-Type": "application/x-www-form-urlencoded",
	}

	if params != nil && params.PageSize != nil {
		data.Set("pageSize", fmt.Sprint(*params.PageSize))
	}
	if params != nil && params.PageToken != nil {
		data.Set("pageToken", *params.PageToken)
	}

	if pageToken != "" {
		data.Set("PageToken", pageToken)
	}

	resp, err := c.requestHandler.Get(c.baseURL+path, data, headers, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListKnowledgeChunksResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[ListKnowledgeChunksResponse](
		*ps,             // The page object
		resp.StatusCode, // HTTP status code
		resp.Header,     // HTTP headers
	)

	return metadataWrapper, nil
}

// Lists KnowledgeChunks records from the API as a list. Unlike stream, this operation is eager and loads 'limit' records into memory before returning.
func (c *ApiService) ListKnowledgeChunks(KbId string, KnowledgeId string, params *ListKnowledgeChunksParams) ([]KnowledgeChunk, error) {
	response, errors := c.StreamKnowledgeChunks(KbId, KnowledgeId, params)

	records := make([]KnowledgeChunk, 0)
	for record := range response {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	return records, nil
}

// ListKnowledgeChunksWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) ListKnowledgeChunksWithMetadata(KbId string, KnowledgeId string, params *ListKnowledgeChunksParams) (*metadata.ResourceMetadata[[]KnowledgeChunk], error) {
	response, errors := c.StreamKnowledgeChunksWithMetadata(KbId, KnowledgeId, params)
	resource := response.GetResource()

	records := make([]KnowledgeChunk, 0)
	for record := range resource {
		records = append(records, record)
	}

	if err := <-errors; err != nil {
		return nil, err
	}

	metadataWrapper := metadata.NewResourceMetadata[[]KnowledgeChunk](
		records,
		response.GetStatusCode(), // HTTP status code
		response.GetHeaders(),    // HTTP headers
	)

	return metadataWrapper, nil
}

// Streams KnowledgeChunks records from the API as a channel stream. This operation lazily loads records as efficiently as possible until the limit is reached.
func (c *ApiService) StreamKnowledgeChunks(KbId string, KnowledgeId string, params *ListKnowledgeChunksParams) (chan KnowledgeChunk, chan error) {
	if params == nil {
		params = &ListKnowledgeChunksParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan KnowledgeChunk, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageKnowledgeChunks(KbId, KnowledgeId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		go c.streamKnowledgeChunks(response, params, recordChannel, errorChannel)
	}

	return recordChannel, errorChannel
}

// StreamKnowledgeChunksWithMetadata returns response with metadata like status code and response headers
func (c *ApiService) StreamKnowledgeChunksWithMetadata(KbId string, KnowledgeId string, params *ListKnowledgeChunksParams) (*metadata.ResourceMetadata[chan KnowledgeChunk], chan error) {
	if params == nil {
		params = &ListKnowledgeChunksParams{}
	}
	params.SetPageSize(client.ReadLimits(params.PageSize, params.Limit))

	recordChannel := make(chan KnowledgeChunk, 1)
	errorChannel := make(chan error, 1)

	response, err := c.PageKnowledgeChunksWithMetadata(KbId, KnowledgeId, params, "")
	if err != nil {
		errorChannel <- err
		close(recordChannel)
		close(errorChannel)
	} else {
		resource := response.GetResource()
		go c.streamKnowledgeChunks(&resource, params, recordChannel, errorChannel)
	}

	metadataWrapper := metadata.NewResourceMetadata[chan KnowledgeChunk](
		recordChannel,            // The stream
		response.GetStatusCode(), // HTTP status code from page response
		response.GetHeaders(),    // HTTP headers from page response
	)

	return metadataWrapper, errorChannel
}

func (c *ApiService) streamKnowledgeChunks(response *ListKnowledgeChunksResponse, params *ListKnowledgeChunksParams, recordChannel chan KnowledgeChunk, errorChannel chan error) {
	curRecord := 1

	for response != nil {
		responseRecords := response.Chunks
		for item := range responseRecords {
			recordChannel <- responseRecords[item]
			curRecord += 1
			if params.Limit != nil && *params.Limit < curRecord {
				close(recordChannel)
				close(errorChannel)
				return
			}
		}

		record, err := client.GetNext(c.baseURL+"/v2/KnowledgeBases/{kbId}/Knowledge/{knowledgeId}/Chunks", response, c.getNextListKnowledgeChunksResponse)
		if err != nil {
			errorChannel <- err
			break
		} else if record == nil {
			break
		}

		response = record.(*ListKnowledgeChunksResponse)
	}

	close(recordChannel)
	close(errorChannel)
}

func (c *ApiService) getNextListKnowledgeChunksResponse(nextPageUrl string) (interface{}, error) {
	if nextPageUrl == "" {
		return nil, nil
	}
	resp, err := c.requestHandler.Get(nextPageUrl, nil, nil, c.apiVersion)
	if err != nil {
		return nil, err
	}

	defer resp.Body.Close()

	ps := &ListKnowledgeChunksResponse{}
	if err := json.NewDecoder(resp.Body).Decode(ps); err != nil {
		return nil, err
	}
	return ps, nil
}
